什么时候声明为实例变量：所有对象都有这个属性，这个属性会随着对象的改变而改变
什么时候声明为静态变量：所有对象都有这个属性，这个属性不会随着对象的改变而改变（节约内存）
静态变量在类加载时初始化，内存在方法区中开辟，访问时不需要新建对象
所有static修饰的元素都是静态的，都可以使用类名.方式访问 也可以通过对象.访问
方法什么时候定义为静态的？
    方法描述的是动作，当所有对象执行这个动作时所产生的影响是一致的，那么这个动作已不属于具体对象，可以将这个动作提升到类级别

