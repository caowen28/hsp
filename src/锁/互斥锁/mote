基本介绍：java语言中引入了对象互斥锁的概念，保证共享数据的完整性
        每个对象都对应一个可称为互斥锁的标记，这个标记保证在任何时刻只有一个线程访问该对象
        关键字syncchronized来与对象互斥锁联系，当某个对象用synchronized修饰时表明该对象该对象在任何时刻只能有一个线程访问
        同步的局限性：程序执行效率低
        同步方法（非静态）的锁可以是this，也可以是其他对象（要求是同一对象）
        同步方法（静态）的锁为当前类本身