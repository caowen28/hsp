jvm为每个对象分配一个this
this定义:表示当前类对象
this是一个引用，this是一个变量，this变量中保存了内存地址指向自己，this存储在jvm堆内存对象内部
那个对象调用，this就代表那个对象
this关键字可以访问本类的属性，方法，构造器
 this区分当前类的属性和局部变量
 通过this访问构造器语法：this(参数列表)
        public T(){
        this("jack",10);//必须放在构造器第一行 --》这种方式不会创建新对象，同时又可以调用其他构造方法
            //T的无参构造
        }
        public T(String name int age){
        //有参构造
        }